"""Marketplace Hub HTTP API package.

This package is intentionally independent from Streamlit.  It exposes the
existing ``marketplace_core`` boundaries to web/mobile frontends and dedicated
workers without duplicating business logic.
"""

API_VERSION = "314"
