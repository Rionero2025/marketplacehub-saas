"use client";
import Link from "next/link"; import { FormEvent, useEffect, useState } from "react"; import { useRouter } from "next/navigation"; import { api } from "@/lib/api"; import { Logo } from "@/components/Logo";
export default function Signup(){const r=useRouter();const[plans,setPlans]=useState<string[]>(["starter"]);const[form,setForm]=useState({company_name:"",username:"",password:"",display_name:"",email:"",seller_name:"",legal_name:"",plan_code:"starter",invite_code:"",remember:true});const[err,setErr]=useState("");const[busy,setBusy]=useState(false);useEffect(()=>{api<{plan_codes:string[]}>("/onboarding/plans").then(x=>setPlans(x.plan_codes)).catch(()=>{})},[]);const set=(k:string,v:unknown)=>setForm(x=>({...x,[k]:v}));
const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setErr("");try{await api("/onboarding/signup",{method:"POST",body:JSON.stringify(form)});r.replace("/dashboard")}catch(x){setErr(x instanceof Error?x.message:"Registrazione non riuscita")}finally{setBusy(false)}};
return <div className="authPage"><div className="authVisual"><Logo/><div><span className="eyebrow">Inizia in pochi minuti</span><h1>La tua azienda, i tuoi Seller, il tuo workspace.</h1><p>Il primo Seller viene creato automaticamente. Kaufland e Worten si collegano dal pannello impostazioni.</p></div></div><div className="authPanel"><form className="authCard wide" onSubmit={submit}><h2>Crea il workspace</h2><div className="formGrid"><label>Azienda<input value={form.company_name} onChange={e=>set("company_name",e.target.value)} required/></label><label>Nome visualizzato<input value={form.display_name} onChange={e=>set("display_name",e.target.value)}/></label><label>Username<input value={form.username} onChange={e=>set("username",e.target.value)} required/></label><label>Email<input type="email" value={form.email} onChange={e=>set("email",e.target.value)}/></label><label>Password<input type="password" minLength={8} value={form.password} onChange={e=>set("password",e.target.value)} required/></label><label>Nome Seller<input value={form.seller_name} onChange={e=>set("seller_name",e.target.value)} placeholder="es. Store Italia"/></label><label>Piano<select value={form.plan_code} onChange={e=>set("plan_code",e.target.value)}>{plans.map(p=><option key={p}>{p}</option>)}</select></label><label>Codice invito<input value={form.invite_code} onChange={e=>set("invite_code",e.target.value)} /></label></div>{err&&<div className="errorBox">{err}</div>}<button className="primaryButton" disabled={busy}>{busy?"Creazione…":"Crea account"}</button><small>Hai già un account? <Link href="/login">Accedi</Link></small></form></div></div>}
